# 🔧 دليل تخصيص البلديات والألوان

## 1. تعديل البلديات

البلديات موجودة في ملف `index.html` بقسم JavaScript. البحث عن:

```javascript
const municipalities = {
    'algiers': ['الجزائر', '...',],
    'adrar': ['...'],
    // إلخ
};
```

### لإضافة بلديات جديدة:

```javascript
const municipalities = {
    'wilaya-code': [
        'البلدية 1',
        'البلدية 2',
        'البلدية 3'
    ]
};
```

### مثال - إضافة بلدية إلى الجزائر:

```javascript
'algiers': [
    'الجزائر',
    'باب الوادي',
    'الحراش',
    'البليدة',  // ← بلدية جديدة
    // إلخ...
]
```

---

## 2. تعديل الألوان

### تغيير اللون الأحمر إلى لون آخر:

ابحث عن:
```css
:root {
    --primary-red: #dc143c;        /* غيّر هنا */
    --secondary-red: #e63946;      /* أو هنا */
    --light-red: #ff6b7a;          /* أو هنا */
}
```

### ألوان مشهورة للاستخدام:

| اللون | الكود |
|------|-------|
| أحمر | #dc143c |
| أزرق | #0066cc |
| أخضر | #00aa00 |
| برتقالي | #ff6600 |
| بنفسجي | #9933cc |
| وردي | #ff0066 |

### مثال - تغيير للأزرق:

```css
:root {
    --primary-dark: #1a1a1a;
    --primary-red: #0066cc;        /* تغيير من أحمر إلى أزرق */
    --secondary-red: #0052a3;      /* أزرق ثانوي */
    --light-red: #3385cc;          /* أزرق فاتح */
}
```

---

## 3. تخصيص الانميشنات

### تغيير سرعة الانميشن:

```css
/* البطيء (أكثر من 1 ثانية) */
animation: fadeIn 1.5s ease-out;

/* السريع (أقل من 0.3 ثانية) */
animation: fadeIn 0.2s ease-out;

/* المتوسط (0.5 ثانية) - موصى به */
animation: fadeIn 0.5s ease-out;
```

### أنواع الانميشنات المتاحة:

- `fadeIn` - ظهور تدريجي
- `slideInRight` - انزلاق من اليمين
- `slideInLeft` - انزلاق من اليسار
- `scaleIn` - تكبير مع ظهور
- `pulse` - نبض
- `bounce` - ارتداد
- `glowPulse` - توهج نابضي

### مثال - إضافة انميشن جديد:

```css
@keyframes customAnimation {
    from {
        opacity: 0;
        transform: rotate(0deg);
    }
    to {
        opacity: 1;
        transform: rotate(360deg);
    }
}

/* استخدم الانميشن */
.element {
    animation: customAnimation 1s ease-out;
}
```

---

## 4. تعديل أسعار الشحن

ابحث عن:
```javascript
const SHIPPING_COSTS = {
    'algiers': 500,    // غيّر هنا
    'adrar': 1200,
    // إلخ...
};
```

### تغيير السعر:

```javascript
SHIPPING_COSTS['algiers'] = 300;  // جديد الآن: 300 دج
```

---

## 5. تغيير نص البلاسهولدر والرسائل

ابحث عن:
```html
<input type="tel" placeholder="أدخل رقم هاتفك...">
<!-- غيّر النص هنا -->
```

### في JavaScript:

```javascript
'showFieldError('phone', 'رقم الهاتف غير صحيح');
// غيّر الرسالة هنا
```

---

## 6. نصائح التخصيص السريع

### قائمة سريعة للبحث والاستبدال:

| ابحث عن | استبدل بـ | الغرض |
|--------|---------|--------|
| `--primary-red` | `--primary-blue` | تغيير اللون الأساسي |
| `#dc143c` | `#0066cc` | تغيير الأحمر للأزرق |
| `animation: fadeIn 0.6s` | `animation: fadeIn 1s` | إبطاء الانميشن |
| `0671234567` | `0541234567` | تغيير مثال الهاتف |

---

## 7. اختبار التغييرات

بعد أي تغيير:

1. احفظ الملف (Ctrl+S)
2. أعد تحميل الموقع (F5)
3. افتح F12 Console للتحقق من الأخطاء
4. اختبر الوظائف المتأثرة

---

## 8. ملفات مرجعية

- **UPDATES.md** - شرح التحديثات الحالية
- **README.md** - دليل شامل
- **CHANGES_SUMMARY.txt** - ملخص التغييرات

---

## ✅ اختبر التخصيصات الخاصة بك!

