# 🌿 Root Forest - موقع تجارة إلكترونية احترافي 🚀

## 📱 نظرة عامة

موقع تجارة إلكترونية كامل لمنتج **Root Forest** (مسحوق بروتين طبيعي)، مصمم بشكل احترافي مع جميع الوظائف المطلوبة.

---

## ✨ الميزات الرئيسية

### 1. 🎨 تصميم عصري واحترافي
- ألوان جذابة: أحمر (#dc143c) + وردي فاتح (#ff69b4)
- تخطيط متوازن ومنظم
- رسوم متحركة سلسة
- responsive design (يعمل على جميع الأجهزة)

### 2. 🛒 نظام السلة المتكامل
```javascript
// تخزين تلقائي في LocalStorage
cart = [
  { id: 'root-forest', name: '...', price: 3800, quantity: 1 }
]
```
- إضافة/حذف منتجات
- عداد ديناميكي في الهيدر
- البيانات تبقى بعد إعادة التحميل

### 3. 💳 صفحة دفع احترافية
```
✓ الاسم الكامل
✓ رقم الهاتف (مع التحقق)
✓ اختيار الولاية (12 ولاية جزائرية)
✓ العنوان الكامل
✓ التحكم بالكمية
✓ حساب السعر تلقائياً
```

### 4. 📊 حساب السعر التلقائي
```
السعر = (سعر الوحدة × الكمية) + رسوم التوصيل
     = (3800 × الكمية) + 300
```

### 5. ✅ معالجة الطلبات
- التحقق من البيانات
- إرسال عبر API
- رسائل نجاح/خطأ جميلة
- حفظ في قاعدة البيانات

---

## 🗂️ هيكل الملفات

```
root-forest/
├── index-new.html              # الصفحة الرئيسية 🏠
├── checkout-new.html           # صفحة الدفع 💳
├── style-new.css               # أنماط CSS 🎨
├── script-new.js               # منطق JavaScript 💻
├── server.js                   # السيرفر Express 🚀
├── package.json                # اعتماديات npm
├── data/
│   └── orders.db              # قاعدة البيانات SQLite
└── README.md                   # هذا الملف
```

---

## 🚀 كيفية البدء

### 1. تثبيت الاعتماديات
```bash
npm install
```

### 2. تشغيل السيرفر
```bash
npm start
```

### 3. فتح الموقع
```
http://localhost:3000/index-new.html
```

---

## 📖 شرح الاستخدام

### الصفحة الرئيسية (`index-new.html`)

#### الأزرار المتاحة:
1. **🛒 إضافة إلى السلة**
   - تضيف المنتج إلى السلة
   - تظهر رسالة نجاح (Toast)
   - يظهر Modal إضافي
   - العداد يتحدّث في الهيدر

2. **💳 اشتر الآن**
   - ينقل المستخدم إلى صفحة checkout
   - يحافظ على الكمية

3. **🛒 أيقونة السلة (في الهيدر)**
   - عرض عدد المنتجات
   - دائرة حمراء صغيرة

### صفحة الدفع (`checkout-new.html`)

#### الخطوات:
1. أملأ **الاسم الكامل** (مثال: أحمد محمد علي)
2. أدخل **رقم الهاتف** (مثال: 0661234567)
3. اختر **الولاية** من القائمة
4. أدخل **العنوان الكامل**
5. تحكم بـ **الكمية** (زر + و -)
6. اضغط **إرسال الطلب**

#### حساب السعر التلقائي:
```
سعر الوحدة: 3,800 دج
رسوم التوصيل: 300 دج
الإجمالي: 3,800 + 300 = 4,100 دج (للكمية 1)
```

---

## 🔌 API Endpoints

### الطلبات (Orders)

#### إنشاء طلب جديد
```bash
POST /api/orders
```

**البيانات المرسلة:**
```json
{
  "customer": {
    "fullName": "أحمد محمد",
    "phone": "0661234567",
    "wilaya": "الجزائر",
    "address": "شارع النيل"
  },
  "items": [
    {
      "name": "Root Forest مسحوق بروتين",
      "quantity": 1,
      "price": 3800
    }
  ],
  "total": 4100
}
```

**الرد (نجاح):**
```json
{
  "success": true,
  "message": "تم حفظ الطلب بنجاح",
  "orderId": "ORD-1732891234567",
  "data": {
    "id": 1,
    "customer_name": "أحمد محمد"
  }
}
```

#### جلب جميع الطلبات
```bash
GET /api/orders
```

#### جلب طلب واحد
```bash
GET /api/orders/:id
```

#### تحديث الطلب
```bash
PATCH /api/orders/:id
```

#### حذف الطلب
```bash
DELETE /api/orders/:id
```

#### الإحصائيات
```bash
GET /api/statistics
```

---

## 💾 تخزين LocalStorage

### السلة
```javascript
localStorage.getItem('cart')
// يرجع: [{ id, name, price, quantity }]
```

**مثال:**
```json
[
  {
    "id": "root-forest",
    "name": "Root Forest مسحوق بروتين",
    "price": 3800,
    "quantity": 2
  }
]
```

---

## 🗄️ قاعدة البيانات

### جدول `orders`
```sql
CREATE TABLE orders (
  id INTEGER PRIMARY KEY,
  order_id TEXT UNIQUE,
  customer_name TEXT,
  customer_phone TEXT,
  customer_address TEXT,
  customer_wilaya TEXT,
  items TEXT,
  subtotal REAL,
  shipping_cost REAL,
  total_price REAL,
  status TEXT DEFAULT 'جديد',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 🎨 الألوان المستخدمة

| اللون | الكود | الاستخدام |
|------|------|-----------|
| أحمر أساسي | `#dc143c` | الأزرار الرئيسية |
| وردي | `#ff69b4` | التدرجات والخلفيات |
| أبيض | `#ffffff` | الخلفية |
| أسود | `#1a1a1a` | النص والهيدر |
| رمادي فاتح | `#f5f5f5` | خلفيات قسم |
| أخضر | `#28a745` | نجاح |
| أحمر خطأ | `#dc3545` | أخطاء |

---

## 📱 Responsive Design

✅ **جوال** (< 480px)
- تخطيط عمودي
- أزرار كاملة العرض
- خط أصغر

✅ **جهاز لوحي** (480px - 768px)
- عمودين في الشبكة
- حجم خط متوسط

✅ **كمبيوتر** (768px+)
- أربعة أعمدة
- حجم خط كامل

---

## 🔒 الأمان

✅ التحقق من البيانات (Client-side)
```javascript
- التحقق من الاسم (يجب أن يكون مليء)
- التحقق من الهاتف (صيغة رقمية)
- التحقق من الكمية (أكبر من 0)
```

✅ معالجة الأخطاء
```javascript
- try/catch للأخطاء
- رسائل خطأ واضحة
- إخفاء تفاصيل الخادم
```

---

## 🎯 الوظائف المتقدمة

### Toast Notifications
```javascript
showToast('✓ تمت إضافة المنتج إلى السلة');
```
- تظهر في الزاوية اليمنى السفلى
- تختفي بعد 3 ثوانٍ
- رسالة تأكيد جميلة

### Success Modal
```javascript
showSuccessModal('تمت إضافة المنتج بنجاح!');
```
- نافذة في المنتصف
- أيقونة ✓ خضراء
- زر "تمام" لإغلاق

### Error Modal
```javascript
showErrorModal('رقم الهاتف غير صحيح');
```
- نافذة في المنتصف
- أيقونة ✕ حمراء
- زر "حسناً" لإغلاق

---

## 🔧 التخصيص

### تغيير السعر
```javascript
// في script-new.js
const PRODUCT = {
  price: 3800  // غيّر هنا
};
```

### تغيير الألوان
```css
/* في style-new.css */
:root {
  --primary-color: #dc143c;    /* غيّر هنا */
  --secondary-color: #ff69b4;  /* غيّر هنا */
}
```

### إضافة ولايات
```html
<!-- في checkout-new.html -->
<option value="الدار البيضاء">الدار البيضاء</option>
<option value="جديدة">جديدة</option>
```

---

## 📊 الإحصائيات

### عرض إحصائيات المبيعات
```bash
GET /api/statistics
```

**الرد:**
```json
{
  "success": true,
  "statistics": {
    "total_customers": 5,
    "total_orders": 12,
    "total_revenue": 45600
  }
}
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: الأزرار لا تعمل
```javascript
// تحقق من console (F12)
// ابحث عن أخطاء JavaScript
```

### المشكلة: البيانات لا تحفظ
```javascript
// تحقق من LocalStorage
localStorage.getItem('cart')
```

### المشكلة: الطلب لا ينرسل
```javascript
// تحقق من اتصال الشبكة
// تأكد من تشغيل السيرفر
```

---

## 📝 التعليقات والتوثيق

كل ملف JavaScript يحتوي على:
- ✅ تعليقات بالعربية
- ✅ أسماء دوال واضحة
- ✅ هيكل منظم
- ✅ شرح الوظائف

---

## 🚀 النسخ إلى الإنتاج

### 1. حفظ الملفات
```bash
git add .
git commit -m "النسخة النهائية"
git push origin root
```

### 2. رفع إلى السيرفر
```bash
scp -r root-forest user@vps.com:/var/www/html/
```

### 3. تشغيل على السيرفر
```bash
npm install
npm start
```

---

## 📞 المتطلبات

- Node.js v14+
- npm v6+
- متصفح حديث (Chrome, Firefox, Safari)

---

## ✅ الخلاصة

### تم إنجاز جميع المتطلبات:
1. ✅ تصميم عصري واحترافي
2. ✅ زر إضافة إلى السلة يعمل فعلياً
3. ✅ زر اشتر الآن ينقل إلى checkout
4. ✅ صفحة checkout احترافية
5. ✅ حساب السعر تلقائياً
6. ✅ إرسال الطلب إلى API
7. ✅ نوافذ نجاح وخطأ جميلة
8. ✅ أيقونة سلة محسّنة
9. ✅ كود منظم ونظيف
10. ✅ مسارات نسبية جاهزة للـ VPS

---

**تم إنشاؤه بنجاح! 🎉**

**التاريخ:** 29 نوفمبر 2025
**الإصدار:** 1.0.0
**الحالة:** ✅ جاهز للاستخدام

---

## 📚 المراجع السريعة

- **الصفحة الرئيسية**: `http://localhost:3000/index-new.html`
- **صفحة الدفع**: `http://localhost:3000/checkout-new.html`
- **API**: `http://localhost:3000/api/orders`
- **قاعدة البيانات**: `./data/orders.db`
- **GitHub**: `https://github.com/anismig3/oanis`

---

**استمتع بموقعك الجديد! 🌿✨**
